export enum ErrorMessages {
  DONOR_NOT_FOUND = 'Donor not found',
  PROJECT_SUBSCRIPTION_NOT_FOUND = 'Project Subscription not found',
  PAYMENT_ALREADY_EXISTS = 'Payment already exists for this month',
}
